# ============================================================
# AI OFM Studio — Python зависимости для управляющей программы
# ============================================================
#
# Это зависимости ТОЛЬКО для нашей программы (CLI + webui + клиенты).
# Тяжёлые ML-стэки (ComfyUI, F5-TTS, RVC, FluxGym) ставятся отдельно
# в свои папки — см. install.ps1 (Windows) или MODELS_CHECKLIST.md.
#
# Установка:
#   pip install -r requirements.txt
#
# Требует Python 3.10 — 3.12.
# ============================================================

# --- Базовые зависимости ---
websocket-client>=1.6.0,<2.0        # общение с ComfyUI WebSocket API
requests>=2.31                       # fallback для REST вызовов

# --- Веб-интерфейс ---
gradio>=4.20,<6.0                   # webui.py

# --- Русский TTS (расстановка ударений перед F5-TTS) ---
ruaccent>=1.5                        # CPU, лёгкий, ~100 MB

# --- Утилиты ---
Pillow>=10.0                         # работа с изображениями
numpy>=1.24
